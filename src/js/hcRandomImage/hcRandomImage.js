(function($) {
    $.fn.hcRandomImage = function() {
        const plugin = this;
        const defaultOptions = {
            targetClass : 'target',
            targetCount : 3
        };
    }
}(jQuery));